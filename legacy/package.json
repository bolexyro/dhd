{
  "name": "phone-control-legacy-workspace",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "build": "pnpm --recursive --if-present --sort build",
    "typecheck": "pnpm --filter @dhd/phone-control build && pnpm --recursive --if-present --sort typecheck",
    "test": "pnpm --filter @dhd/phone-control build && pnpm --recursive --if-present --sort test",
    "phone-control-mcp:start": "pnpm --filter @dhd/phone-control-mcp start",
    "phone-control-mcp:dev": "pnpm --filter @dhd/phone-control-mcp dev",
    "phone-control-viewer:start": "pnpm --filter @dhd/phone-viewer start"
  },
  "devDependencies": {
    "@types/node": "22.15.21",
    "tsx": "^4.23.12",
    "typescript": "5.8.3",
    "vitest": "3.2.4"
  }
}
